# --- Butterfly System Environment Variables ---
# Copy this file to .env and fill in your actual values.
# The .env file is ignored by git.

# A unique, 32-byte URL-safe base64-encoded key for encrypting data.
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
BUTTERFLY_ENCRYPTION_KEY=

# A long, random, and secret string used for signing JWTs.
JWT_SECRET_KEY=

# The edition to run. Can be "COMMUNITY" or "HOSTED". Defaults to "COMMUNITY".
BUTTERFLY_EDITION=COMMUNITY